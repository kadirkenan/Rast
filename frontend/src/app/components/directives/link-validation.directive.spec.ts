import { LinkValidationDirective } from './link-validation.directive';

describe('LinkValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new LinkValidationDirective();
    expect(directive).toBeTruthy();
  });
});
